# CLT shiny app
# mb

library(shiny)

shinyServer(function(input, output) {
  
  if(input$pDist == "rnorm") {
    
  }
  
  if(input$pDist == "runif") {
    
  }
  
  if(input$pDist == "rexp") {
    
  }
  
  if(input$pDist == "rpois") {
    
  }
  
})